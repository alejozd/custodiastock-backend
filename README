# CustodiaStock

Sistema web para **registro y trazabilidad de entregas de productos en custodia** dentro de una empresa.

Permite registrar quién entrega, quién recibe, qué producto se entrega y almacenar la **firma digital del receptor**, funcionando como una **bitácora digital de custodia de inventario**.

Este sistema está pensado para empresas donde los productos se mueven entre personas (bodega, técnicos, vendedores, instaladores, etc.) y se necesita saber **quién tiene cada elemento en cada momento**.

---

# Problema que resuelve

En muchas empresas pequeñas o medianas:

* los productos se entregan verbalmente
* se anotan en cuadernos
* no se sabe quién tiene el producto
* se pierden elementos
* no hay trazabilidad

CustodiaStock permite registrar digitalmente cada entrega y conservar evidencia mediante **firma del receptor**.

---

# Características principales

* Autenticación con **JWT**
* Registro de **usuarios**
* Registro de **productos**
* Registro de **entregas**
* Firma digital del receptor (imagen)
* Historial de entregas
* Cancelación de entregas (solo administrador)
* Documentación automática con **Swagger**

---

# Arquitectura del sistema

El proyecto está construido con una arquitectura moderna basada en servicios.

```
Frontend (React + Vite)
        │
        │ HTTP REST API
        ▼
Backend (Node.js + Express)
        │
        │ ORM
        ▼
Prisma
        │
        ▼
MySQL Database
```

La aplicación se ejecuta utilizando **Docker** para simplificar despliegue y portabilidad.

---

# Stack tecnológico

Backend

* Node.js
* Express
* Prisma ORM
* MySQL
* JWT Authentication
* Swagger API Docs

Frontend

* React
* Vite

Infraestructura

* Docker
* Docker Compose

---

# Estructura del proyecto

```
CustodiaStock
│
├── backend
│   ├── src
│   │   ├── controllers
│   │   ├── routes
│   │   ├── services
│   │   ├── middleware
│   │   └── server.js
│   │
│   └── prisma
│       └── schema.prisma
│
├── frontend
│
└── docker-compose.yml
```

---

# Ejecutar el proyecto

## 1. Clonar el repositorio

```
git clone https://github.com/TU_USUARIO/custodiastock-backend
```

---

## 2. Configurar variables de entorno

Crear archivo `.env` dentro de `backend`.

```
DATABASE_URL="mysql://root:root@localhost:3306/custodia_db"
JWT_SECRET=super-secret-key
JWT_EXPIRES_IN=8h
CORS_ORIGIN=http://localhost:5173
```

> `CORS_ORIGIN` accepts a comma-separated list of allowed origins (e.g.
> `http://localhost:5173,https://app.midominio.com`). If omitted, it defaults
> to `http://localhost:5173` (the local Vite dev server) — it never falls back
> to `*`.

> `TRUST_PROXY_HOPS`: only set this if the app runs behind a reverse proxy
> (nginx, load balancer, etc.). It must match the number of proxy hops in
> front of the app, so `express-rate-limit` reads the real client IP from
> `X-Forwarded-For` instead of the proxy's IP. Leave it unset in a direct
> deployment (no proxy) — trusting a proxy that doesn't exist lets clients
> spoof their own IP and bypass the `/login` rate limiter.

> `SWAGGER_USER` / `SWAGGER_PASSWORD`: protect `/api/docs` with HTTP Basic
> Auth. When both are set, the browser is prompted for these credentials
> before Swagger UI loads. When either is unset, `/api/docs` stays open and a
> warning is logged once at server startup — set both in any environment
> where the API docs shouldn't be publicly reachable.

---

## 3. Generar Prisma Client

Antes de hacerlo asegúrate de **detener cualquier proceso Node**.

```
npm run prisma:generate
```

---

## 4. Sincronizar base de datos

```
npm run prisma:sync
```

---

## 5. Ejecutar backend

```
npm run dev
```

Servidor:

```
http://localhost:3000
```

---

# Documentación de la API

Swagger UI disponible en:

```
http://localhost:3000/api/docs
```

Permite probar todos los endpoints directamente desde el navegador.

---

# Ejemplo de flujo de uso

### 1 Login

```
POST /api/auth/login
```

```
{
  "email": "carlos@empresa.com",
  "password": "Secure123!"
}
```

Respuesta:

```
{
  "token": "JWT_TOKEN"
}
```

Enviar el token en todas las peticiones:

```
Authorization: Bearer JWT_TOKEN
```

---

# Ejemplo de registro de entrega

```
POST /api/deliveries
```

```
{
  "productId": 1,
  "quantity": 3,
  "deliveredById": 1,
  "receivedById": 2,
  "signatureImage": "data:image/png;base64,iVBOR..."
}
```

Esto registra:

* producto
* cantidad
* quién entrega
* quién recibe
* firma digital del receptor

---

# Roles de usuario

* ADMIN
* OPERATOR

ADMIN puede:

* cancelar entregas
* administrar usuarios

---

# Roadmap (mejoras futuras)

* Inventario disponible por producto
* Reportes de entregas
* Búsqueda avanzada
* Adjuntar fotos del producto
* Exportar reportes PDF
* Notificaciones
* Auditoría completa de movimientos

---

# Autor

Proyecto desarrollado por **Alejandro Zambrano**.

Ingeniero de Sistemas especializado en desarrollo backend y arquitectura de aplicaciones.

---
